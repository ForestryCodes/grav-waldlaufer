<ul class="header__nav-list">
{% for page in pages.children.visible %}
    {% set current_page = (page.active or page.activeChild) ? '--active' : '' %}
    <li class="header__nav-item{{ current_page }}"><a href="{{ page.url }}">{{ page.menu }}</a></li>
{% endfor %}
</ul>